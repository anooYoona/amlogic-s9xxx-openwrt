# ==========================================
# 1. 基础系统与管理界面（中文 + 终端 + 刚才的磁盘管理）
# ==========================================
CONFIG_PACKAGE_luci=y
CONFIG_PACKAGE_luci-base=y
CONFIG_PACKAGE_luci-theme-material=y
CONFIG_PACKAGE_luci-i18n-base-zh-cn=y
CONFIG_PACKAGE_luci-app-package-manager=y
CONFIG_PACKAGE_luci-i18n-package-manager-zh-cn=y
CONFIG_PACKAGE_luci-app-ttyd=y
CONFIG_PACKAGE_luci-i18n-ttyd-zh-cn=y
CONFIG_PACKAGE_luci-app-diskman=y
CONFIG_PACKAGE_luci-i18n-diskman-zh-cn=y

# ==========================================
# 2. 晶晨宝盒（核心：N1 在线升降级固件/内核）
# ==========================================
CONFIG_PACKAGE_luci-app-amlogic=y
CONFIG_PACKAGE_luci-i18n-amlogic-zh-cn=y

# ==========================================
# 3. 旁路由网络核心（防火墙 + IPv6 客户端 + 底层转发）
# ==========================================
CONFIG_PACKAGE_luci-app-firewall=y
CONFIG_PACKAGE_luci-i18n-firewall-zh-cn=y
CONFIG_PACKAGE_odhcp6c=y
CONFIG_PACKAGE_kmod-tun=y

# ==========================================
# 4. 智能家居 Docker 套件
# ==========================================
CONFIG_PACKAGE_dockerd=y
CONFIG_PACKAGE_docker=y
CONFIG_PACKAGE_docker-compose=y
CONFIG_PACKAGE_luci-app-dockerman=y
CONFIG_PACKAGE_luci-i18n-dockerman-zh-cn=y
CONFIG_BUSYBOX_DEFAULT_FALLOCATE=y

# ==========================================
# 5. 异地组网 Tailscale
# ==========================================
CONFIG_PACKAGE_tailscale=y

# ==========================================
# 6. 科学上网（OpenClash + PassWall）
# ==========================================
CONFIG_PACKAGE_luci-app-openclash=y
CONFIG_PACKAGE_luci-app-passwall=y
CONFIG_PACKAGE_luci-i18n-passwall-zh-cn=y

# ==========================================
# 7. 实用排错与维护工具（传文件SFTP + 证书 + 监控）
# ==========================================
CONFIG_PACKAGE_openssh-sftp-server=y
CONFIG_PACKAGE_ca-certificates=y
CONFIG_PACKAGE_curl=y
CONFIG_PACKAGE_htop=y
CONFIG_PACKAGE_block-mount=y
CONFIG_PACKAGE_kmod-fs-ext4=y
CONFIG_PACKAGE_kmod-fs-vfat=y
CONFIG_PACKAGE_tcpdump=y

# ==========================================
# 8. 强行剔除底包捆绑的无用 4G 网卡协议包
# ==========================================
CONFIG_PACKAGE_-luci-proto-ncm=y
CONFIG_PACKAGE_-comgt-ncm=y
